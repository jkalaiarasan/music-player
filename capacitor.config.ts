import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Music Player Vue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
